(alias bg       background)
(alias black-bg bg 0 0 0)

(def r (-> (time)
         (* 4) (sin) (* 0.2) (+ 0)))

(black-bg)

(-@> (cube 1)
     ;(translate 1)
     (no-fill)
     (rotate-x (% (time) (* 2 pi)))
     ;(scale r r)
     (color 0 1 0))
